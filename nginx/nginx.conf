server {
    listen 80;
    server_name foodonline.run.place www.foodonline.run.place;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 "Temporary HTTP for Certbot challenge";
    }
}
