

services:
  web:
    build: .
    
    volumes:
      - .:/app
      
    ports:
      - "8000:8000"
    env_file:
      - .env.web
    environment:
      - DJANGO_DEBUG=False
      - GDAL_DATA=/usr/share/gdal
      - DJANGO_SETTINGS_MODULE=mysite.settings.settings
      
      
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             daphne -b 0.0.0.0 -p 8000 mysite.asgi:application"



  

  

  

  nginx:
    build: ./nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
     
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    
    restart:  unless-stopped  
    depends_on:
      - web

  certbot:
    image:  certbot/certbot
    volumes:
     - ./certbot/www:/var/www/certbot   
     - ./certbot/conf:/etc/letsencrypt 

    command: certonly --webroot -w /var/www/certbot --email mahasethmanish63@gmail.com --agree-tos --no-eff-email -d foodonline.run.place -d www.foodonline.run.place --non-interactive
    restart: unless-stopped
    depends_on:
      - nginx 
    

  